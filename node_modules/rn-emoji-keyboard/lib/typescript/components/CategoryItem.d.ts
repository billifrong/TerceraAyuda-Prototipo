import type { CategoryNavigationItem, CategoryTypes } from '../types';
declare type CategoryItemProps = {
    item: CategoryNavigationItem;
    index: number;
    handleScrollToCategory: (category: CategoryTypes) => void;
};
export declare const CategoryItem: ({ item, index, handleScrollToCategory }: CategoryItemProps) => JSX.Element;
export {};
