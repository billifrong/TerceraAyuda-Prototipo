import type { IconNames } from '../types';
export declare const Icon: ({ iconName, isActive, normalColor, activeColor, }: {
    iconName: IconNames | 'Close' | 'QuestionMark';
    isActive: boolean;
    normalColor: string;
    activeColor: string;
}) => JSX.Element | null;
