import * as React from 'react';
import { ModalProps } from 'react-native';
declare type ModalWithBackdropProps = {
    isOpen: boolean;
    backdropPress: () => void;
    children: React.ReactNode;
};
export declare const ModalWithBackdrop: ({ isOpen, backdropPress, children, ...rest }: ModalWithBackdropProps & ModalProps) => JSX.Element;
export {};
