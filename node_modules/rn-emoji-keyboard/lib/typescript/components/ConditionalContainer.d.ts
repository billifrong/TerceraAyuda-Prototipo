import { ReactNode } from 'react';
import { ViewProps } from 'react-native';
declare type ConditionalContainerTypes = {
    children: ReactNode;
    container: (children: ReactNode) => ReactNode;
    condition: boolean;
};
export declare const ConditionalContainer: ({ children, container, condition, }: ConditionalContainerTypes) => JSX.Element;
declare type IsSafeAreaWrapperProps = {
    children: ReactNode;
    isSafeArea: boolean;
} & ViewProps;
export declare const IsSafeAreaWrapper: ({ children, isSafeArea, ...props }: IsSafeAreaWrapperProps) => JSX.Element;
export {};
